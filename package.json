{
  "name": "etai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test '*/tests/*.test.js'",
    "start": "node messaging/index.js",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=18"
  }
}
