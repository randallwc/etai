{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Persona",
  "description": "The UI-facing agent shape. Produced from an Ambiguous coworker or defined locally in src/agents.js as a fallback.",
  "type": "object",
  "required": ["id", "name", "role", "theme", "initials", "greeting"],
  "properties": {
    "id": { "type": "string", "description": "Ambiguous user id, or local slug for fallbacks" },
    "name": { "type": "string", "description": "display_name" },
    "role": { "type": "string" },
    "tagline": { "type": "string" },
    "theme": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" },
    "initials": { "type": "string", "maxLength": 2 },
    "greeting": { "type": "string", "description": "First line spoken on connect" },
    "summary": { "type": ["string", "null"], "description": "Canned day summary; null means fetch from Ambiguous" },
    "taskAck": { "type": "string" },
    "skills": {
      "type": "array",
      "items": { "type": "string" },
      "description": "What this agent is good at; shown as chips under the agent name"
    },
    "voice": {
      "type": "object",
      "properties": {
        "pitch": { "type": "number", "minimum": 0, "maximum": 2 },
        "rate": { "type": "number", "minimum": 0.5, "maximum": 2 }
      },
      "description": "speechSynthesis utterance tuning — gives each agent a distinct voice"
    }
  }
}
