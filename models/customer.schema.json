{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Customer",
  "description": "A client who texts the agent's number. name is learned over time and may be null. Backed by an Ambiguous CRM contact. Defined by the data model in docs/API.md.",
  "type": "object",
  "required": ["id", "phone"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "name": { "type": ["string", "null"] },
    "phone": { "type": "string", "pattern": "^\\+[1-9]\\d{6,14}$" },
    "address": { "type": ["string", "null"] },
    "ambiguousCrmId": { "type": ["string", "null"], "description": "Ambiguous CRM contact id once upserted" },
    "notes": { "type": "string" }
  },
  "additionalProperties": false
}
